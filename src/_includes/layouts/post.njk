---
layout: layouts/base.njk
tags: post
---
{% set postAuthor = author | default(meta.author, true) %}

{% if images.feature %}
  <figure class="w-screen h-48 -mx-12 -mt-12 sm:h-64 lg:h-96 sm:-mx-20 lg:-mx-32 ">
    <img class="object-cover w-full h-full" src="/assets/images/{{ images.feature }}" alt="">
  </figure>
{% endif %}

<article class="mt-12">
  <h1>{{ title }}</h1>
  <p>
    <small>
      <time datetime="{{ date | dateToFormat("yyyy-MM-dd") }}">
        {{ date | dateToFormat("dd LLL yyyy") }}
      </time>
    </small>
  </p>

  {{ layoutContent | safe }}

<section>
<ol>
      {%- for tag in tags | filterTagList -%}
      {%- set tagUrl %}/tags/{{ tag | slugify }}/{% endset -%}
      <li>#<a href="{{ tagUrl | url }}" class="post-tag">{{ tag }}</a></li>
      {%- endfor %}
      </ol>
</section>

{# <section>
  <h2>Webmentions</h3>
  {% set webmentionUrl %}{{ page.url | url | absoluteUrl(site.url) }}{% endset %}
  {% include 'webmentions.njk' %}
</section> #}
</article>