<!DOCTYPE html>
<html lang="{{ meta.lang }}">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ seo.title or title }} | {{ meta.title }}</title>
    {%- include "partials/head.njk" %}
    <link rel="stylesheet" href="/assets/css/style.css" defer/>
    {% set prismTomorrow %}{% include "prism/prism-tomorrow.min.css" %}{% endset -%}
    <style>{{- prismTomorrow | cssmin | safe }}</style>
    <script src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.x.x/dist/alpine.min.js" defer></script>

    {% if meta.netlifyCMS == true %}
    {% include "partials/admin.njk" %}
    {% endif %}

  </head>
  <body>
    <div class="flex flex-col min-h-screen">
      {% include "components/nav.njk" %}
      <div class="flex-grow mx-4 my-12 sm:mx-20 lg:mx-32 ">
        <main class="prose sm:prose-lg" id="content">
          {{ content | safe }}
        </main>
      </div>
      {% include "partials/footer.njk" %}
    </div>
  </body>
</html>